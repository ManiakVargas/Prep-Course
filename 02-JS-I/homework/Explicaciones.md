* Variables
Sirve para almacenar un valor para usarse más tarde, se define con la palabra "VAR"
y se usa un signo "=" para asignarle un valor a la variable.

 * Strings
 es una linea de texto que se define con comillas
   var MiNombre = 'Emmanuel'  

 * Funciones (argumentos, `return`)
agrupa codigo que se va usar muchas veces, lo guarda, se invoca y se ejecuta. 

 * Declaraciones `if`
sirven para el control de flujo de las funciones.

 * Valores booleanos (`true`, `false`) 
sirven para hacer declaraciones en las funciones.